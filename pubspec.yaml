name: app_ai_sound_separation
description: "AI音频分离 - 智能音轨分离工具"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.8.4
  dio: ^5.7.0
  file_picker: ^8.1.7
  just_audio: ^0.9.43
  path_provider: ^2.1.5
  flutter_dotenv: ^5.2.1
  webview_flutter: ^4.11.0
  uuid: ^4.5.1
  flutter_secure_storage: ^9.2.4
  shared_preferences: ^2.3.5
  permission_handler: ^11.3.1
  intl: ^0.20.2
  url_launcher: ^6.3.1
  share_plus: ^10.1.4
  image_picker: ^1.1.2
  audio_decoder: ^0.7.4
  http: ^1.6.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - .env
